<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recipes.mapper.TagMapper">

    <select id="selectByRecipeId" resultType="com.recipes.entity.Tag">
        SELECT t.*
        FROM recipe_tag t
        INNER JOIN recipe_tag_relation r ON t.id = r.tag_id
        WHERE r.recipe_id = #{recipeId}
        AND t.deleted = 0
    </select>

</mapper>
