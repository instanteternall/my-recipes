<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recipes.mapper.IngredientMapper">

    <select id="selectByUserId" resultType="com.recipes.entity.Ingredient">
        SELECT * FROM ingredient
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="selectByKeyword" resultType="com.recipes.entity.Ingredient">
        SELECT * FROM ingredient
        WHERE deleted = 0
          AND name LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
        LIMIT 20
    </select>

</mapper>
