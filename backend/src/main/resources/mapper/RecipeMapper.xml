<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recipes.mapper.RecipeMapper">

    <select id="selectByUserId" resultType="com.recipes.entity.Recipe">
        SELECT * FROM recipe
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="selectByKeyword" resultType="com.recipes.entity.Recipe">
        SELECT * FROM recipe
        WHERE deleted = 0
          AND (
            name LIKE CONCAT('%', #{keyword}, '%')
            OR description LIKE CONCAT('%', #{keyword}, '%')
          )
        ORDER BY view_count DESC, created_at DESC
        LIMIT 20
    </select>

</mapper>
